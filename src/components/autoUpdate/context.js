import { createContext } from "react";

export const WorkoutContext = createContext(undefined);
export const ExerciseContext = createContext(undefined);
export const OriginalDaysContext = createContext(undefined);